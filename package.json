{
  "name": "eoca",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy-static": "aws s3 sync ./dist s3://eoca/site --exclude \"*.json\" --storage-class REDUCED_REDUNDANCY",
    "deploy-data": "aws s3 sync ./dist s3://eoca/site --exclude \"*\" --include \"*.json\" --content-type application/json --storage-class REDUCED_REDUNDANCY",
    "deploy": "npm run build && npm run deploy-data && npm run deploy-static"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.3.9"
  }
}
